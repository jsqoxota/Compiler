{
    int i;
    float j;
    double k;
    float[100] x;
    while(true){
        v = x;
        do { i = i + 1; }while (a[j] < v);
        do { j = j - 1; }while (a[j] > v);
        if ( i >= j)break;
        x = a[i]; a[i] = a[j];  a[j] = x;
    }
}